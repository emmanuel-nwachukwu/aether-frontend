services:
  - type: web
    name: aether-finance
    env: static
    staticPublishPath: dist
    buildCommand: npm install && npm run build
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
